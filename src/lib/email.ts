// Email module — ready for future email provider configuration
// Currently no email provider is configured. When one is added, this module can be activated.

console.log('[EMAIL] No email provider configured — OTP system is disabled');
